export interface Product {
    id: string,
    name: string,
    can_expire: boolean,
    expiry_date: string,
    description: string,
    category: string,
    price: number,
    on_special: boolean,
    image_url: string
}

